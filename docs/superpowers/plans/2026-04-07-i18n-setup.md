# i18n Setup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Обновить зависимости и добавить локализацию через next-intl (ru/en, префикс в URL) с примером на главной и свитчером языка.

**Architecture:** App Router + next-intl с `localePrefix: 'always'`. Все страницы переезжают под `app/[locale]/`. Корневой `app/layout.tsx` остаётся минимальным (passthrough), `<html>` рендерится в `[locale]/layout.tsx`. Middleware от next-intl обрабатывает редиректы.

**Tech Stack:** Next.js 15 (App Router), React 19, TypeScript, pnpm, next-intl.

---

## Task 1: Обновить зависимости

**Files:**
- Modify: `package.json`, `pnpm-lock.yaml`

- [ ] **Step 1: Обновить все зависимости до latest**

```bash
pnpm update --latest
```

- [ ] **Step 2: Установить зависимости заново**

```bash
pnpm install
```

- [ ] **Step 3: Прогнать линт/типы/формат**

```bash
pnpm pp
```

Expected: Проходит без ошибок. Если есть breaking changes — починить (типичные: новые eslint правила, deprecation warnings). Если затронуто много кода — остановиться и сообщить пользователю перед продолжением.

- [ ] **Step 4: Проверить сборку**

```bash
pnpm build
```

Expected: Сборка успешна.

- [ ] **Step 5: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "chore: update dependencies to latest"
```

---

## Task 2: Установить next-intl

**Files:**
- Modify: `package.json`, `pnpm-lock.yaml`

- [ ] **Step 1: Установить next-intl**

```bash
pnpm add next-intl
```

- [ ] **Step 2: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "chore: add next-intl"
```

---

## Task 3: Создать messages

**Files:**
- Create: `messages/ru.json`
- Create: `messages/en.json`

- [ ] **Step 1: Создать `messages/ru.json`**

```json
{
  "Home": {
    "title": "Привет, мир!",
    "description": "Это пример страницы с локализацией через next-intl."
  },
  "LanguageSwitcher": {
    "label": "Язык",
    "ru": "Русский",
    "en": "English"
  }
}
```

- [ ] **Step 2: Создать `messages/en.json`**

```json
{
  "Home": {
    "title": "Hello, world!",
    "description": "This is an example page localized via next-intl."
  },
  "LanguageSwitcher": {
    "label": "Language",
    "ru": "Русский",
    "en": "English"
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add messages
git commit -m "feat(i18n): add ru/en message catalogs"
```

---

## Task 4: i18n routing и navigation

**Files:**
- Create: `i18n/routing.ts`
- Create: `i18n/navigation.ts`

- [ ] **Step 1: Создать `i18n/routing.ts`**

```ts
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['ru', 'en'],
  defaultLocale: 'ru',
  localePrefix: 'always',
});
```

- [ ] **Step 2: Создать `i18n/navigation.ts`**

```ts
import { createNavigation } from 'next-intl/navigation';

import { routing } from './routing';

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

- [ ] **Step 3: Commit**

```bash
git add i18n
git commit -m "feat(i18n): add routing and navigation helpers"
```

---

## Task 5: Request config

**Files:**
- Create: `i18n/request.ts`

- [ ] **Step 1: Создать `i18n/request.ts`**

```ts
import { hasLocale } from 'next-intl';
import { getRequestConfig } from 'next-intl/server';

import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested)
    ? requested
    : routing.defaultLocale;

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```

- [ ] **Step 2: Commit**

```bash
git add i18n/request.ts
git commit -m "feat(i18n): add request config"
```

---

## Task 6: Middleware

**Files:**
- Create: `middleware.ts`

- [ ] **Step 1: Создать `middleware.ts`**

```ts
import createMiddleware from 'next-intl/middleware';

import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  matcher: ['/((?!api|_next|_vercel|.*\\..*).*)'],
};
```

- [ ] **Step 2: Commit**

```bash
git add middleware.ts
git commit -m "feat(i18n): add locale middleware"
```

---

## Task 7: Подключить плагин next-intl в next.config

**Files:**
- Modify: `next.config.mjs`

- [ ] **Step 1: Заменить содержимое `next.config.mjs`**

```js
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  reactStrictMode: true,
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 2: Commit**

```bash
git add next.config.mjs
git commit -m "feat(i18n): wire next-intl plugin"
```

---

## Task 8: Перенести страницы под `app/[locale]/`

**Files:**
- Create: `app/[locale]/layout.tsx`
- Create: `app/[locale]/page.tsx`
- Modify: `app/layout.tsx` (минимизировать)
- Delete: `app/page.tsx` (переезжает под `[locale]`)
- Move: `app/dynamic-route` → `app/[locale]/dynamic-route` (если нужно сохранить локализованным)

- [ ] **Step 1: Переместить `app/page.tsx` в `app/[locale]/page.tsx`**

```bash
mkdir -p app/\[locale\]
git mv app/page.tsx app/\[locale\]/page.tsx
git mv app/dynamic-route app/\[locale\]/dynamic-route
```

- [ ] **Step 2: Переписать `app/[locale]/page.tsx`** на использование переводов

```tsx
import { getTranslations } from 'next-intl/server';

import LanguageSwitcher from '@/components/LanguageSwitcher';

import styles from '../page.module.css';

export default async function Home() {
  const t = await getTranslations('Home');

  return (
    <main className={styles.main} data-testid="home-page">
      <h1>{t('title')}</h1>
      <p>{t('description')}</p>
      <LanguageSwitcher />
    </main>
  );
}
```

(Если в проекте нет alias `@/`, заменить на относительный путь `../../components/LanguageSwitcher`. Проверить `tsconfig.json`.)

- [ ] **Step 3: Создать `app/[locale]/layout.tsx`**

```tsx
import { hasLocale, NextIntlClientProvider } from 'next-intl';
import { setRequestLocale } from 'next-intl/server';
import { Inter } from 'next/font/google';
import { notFound } from 'next/navigation';

import type { Metadata } from 'next';

import { routing } from '@/i18n/routing';

import '../globals.css';
import Providers from '../providers';

const inter = Inter({ subsets: ['latin', 'cyrillic'] });

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  if (!hasLocale(routing.locales, locale)) {
    notFound();
  }
  setRequestLocale(locale);

  return (
    <html lang={locale}>
      <body className={inter.className}>
        <NextIntlClientProvider>
          <Providers>{children}</Providers>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 4: Минимизировать корневой `app/layout.tsx`**

next-intl документирует, что `<html>` должен быть в `[locale]/layout.tsx`. Корневой layout удалить нельзя в App Router. Заменить содержимое `app/layout.tsx`:

```tsx
export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
```

- [ ] **Step 5: Commit**

```bash
git add app
git commit -m "feat(i18n): move pages under [locale] segment"
```

---

## Task 9: LanguageSwitcher

**Files:**
- Create: `components/LanguageSwitcher.tsx`

- [ ] **Step 1: Создать `components/LanguageSwitcher.tsx`**

```tsx
'use client';

import { useLocale, useTranslations } from 'next-intl';
import { useTransition } from 'react';

import { routing } from '@/i18n/routing';
import { usePathname, useRouter } from '@/i18n/navigation';

export default function LanguageSwitcher() {
  const t = useTranslations('LanguageSwitcher');
  const locale = useLocale();
  const router = useRouter();
  const pathname = usePathname();
  const [isPending, startTransition] = useTransition();

  const switchTo = (next: (typeof routing.locales)[number]) => {
    startTransition(() => {
      router.replace(pathname, { locale: next });
    });
  };

  return (
    <div aria-label={t('label')} data-testid="language-switcher">
      {routing.locales.map((l) => (
        <button
          key={l}
          type="button"
          disabled={l === locale || isPending}
          onClick={() => switchTo(l)}
        >
          {t(l)}
        </button>
      ))}
    </div>
  );
}
```

Если alias `@/` не настроен — использовать относительные пути.

- [ ] **Step 2: Commit**

```bash
git add components
git commit -m "feat(i18n): add language switcher"
```

---

## Task 10: Проверка

- [ ] **Step 1: Прогнать lint/types/format**

```bash
pnpm pp
```

Expected: проходит.

- [ ] **Step 2: Сборка**

```bash
pnpm build
```

Expected: успех.

- [ ] **Step 3: Запустить dev и проверить вручную**

```bash
pnpm dev
```

Открыть `http://localhost:3000`:
- редиректит на `/ru`
- видно `Привет, мир!` и описание на русском
- кнопка `English` активна, `Русский` disabled
- клик на `English` → URL `/en`, текст на английском
- клик на `Русский` → URL `/ru`, текст обратно на русском

- [ ] **Step 4: Если всё ок — финальный commit (если есть несохранённые правки)**

```bash
git status
```

Если есть untracked/modified — закоммитить с осмысленным сообщением.
